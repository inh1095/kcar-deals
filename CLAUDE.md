# kcar-deals

K카(kcar.com) 국산 중고차 매물을 수집해 시세 대비 "꿀매물"을 점수화하고 GitHub Pages로 공유하는 개인 프로젝트. 한국어, 이모지 없이, 수치 중심으로 짧게.

작업 전 `WORKORDER.md`를 끝까지 읽는다. 다른 프로젝트(블리비 등) 파일·세션은 참조하지 않는다.

규칙:
- `kcar.com/robots.txt`와 이용약관 허용 범위 안에서만. 요청 간 1.5~2초, 동시 요청 금지. 차단·캡차가 나오면 우회하지 말고 멈추고 기록.
- 원본 수집물(`data/raw/`)은 커밋하지 않는다(gitignore). 커밋하는 데이터는 정리본 `data/listings.csv`와 `docs/`뿐.
- 공개 사이트(`docs/`)에는 분석 표(차명·연식·주행거리·가격·사고·점수·K카 링크)만 싣는다. 사진은 싣지 않는다(핫링크 금지). `noindex`, 출처·수집 일시·면책 문구를 유지한다.
- 수집·점수 스크립트는 LLM 없이 실행돼야 한다(`python search_kcar.py ...`). LLM은 만드는 데만 쓴다.
- 도구 파일 수정 전 `.bak.YYYY-MM-DD` 백업. 브랜치 `main`에 직접 커밋·push(다른 작성자 없음). 마일스톤마다 `PROGRESS.md` 갱신 후 push.
- 질문 없이 끝까지 진행. 막히면 대안 2~3개를 시도하고 결과와 함께 기록.
