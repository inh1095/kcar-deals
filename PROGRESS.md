# PROGRESS

개인 검토용 비공식 분석. 출처 K카(kcar.com). 원본 응답은 `data/raw/`에 두고 커밋하지 않는다.

## 0. 준수 범위 확인 (2026-09-08)

`https://www.kcar.com/robots.txt` (HTTP 200) 확인 결과:

```
User-agent:*
Allow: /
Disallow: /html/  /js/  /pub/  /mp/  /mb/  /cs/csVoc  /account/
Disallow: /bc/LatelyViewedCar/list
Disallow: /car/info/
Disallow: /bc/detail/carInfoDtl?
Disallow: /br/detail/brandCarInfoDtl?
Disallow: /ur/RentDtl?
```

판단:

- 목록/검색 페이지 `\/bc\/search` 는 **Allow** (sitemap.xml에도 priority 0.9로 등재). 목록 수집은 허용 범위.
- 프런트 번들 `\/_nuxt\/*.js` 는 Disallow 목록에 없음 → 엔드포인트 파악 목적의 조회 허용. (`\/js\/` 는 Disallow 이므로 건드리지 않음.)
- **상세 페이지 `\/bc\/detail\/carInfoDtl?` 와 `\/car\/info\/` 는 Disallow.** 따라서 상세 페이지는 **한 건도 요청하지 않았다.** 상세에서만 얻을 수 있는 필드(총 구매비용, 보험 이력, 소유자 변경 횟수, 등록일, 보증)는 `null`로 두고, 사이트에는 상세 링크만 게시한다(링크는 크롤링이 아님).
- `api.kcar.com/robots.txt` 는 404(제한 명시 없음). 이 API는 Allow 대상인 `\/bc\/search` 페이지가 목록을 그리기 위해 호출하는 동일 데이터원이다.
- 요청 예절: 순차 요청만(동시 요청 없음), 간격 1.8초, User-Agent 고정 1개. 403/429/캡차 발생 시 우회 없이 즉시 중단하도록 코드에 구현.

이용약관: kcar.com 이용약관 페이지는 SPA 라우팅이라 정적 HTML에서 본문을 얻지 못했다. 본 작업은 (1) robots.txt 허용 경로만 조회, (2) 개인 구매 검토 목적의 비상업적 분석, (3) 원본 재배포 없음(사진·연락처·원본 JSON 미게시), (4) 분석 표와 K카 상세 링크만 공개 — 로 범위를 제한했다. 상업적 이용 금지 문구를 산출물 전면에 유지한다.

## 1. 마일스톤 1 — API 파악 (완료)

K카는 Nuxt(SSR) SPA이고 `\/bc\/search` HTML에는 매물이 없다(`__NUXT__` 상태에 목록 없음, 클라이언트 페치). 순서대로 해결:

1. `curl`로 `\/bc\/search` HTML → `<script src="\/_nuxt\/*.js">` 9개 수집.
2. 번들에서 `.post("/...")` 문자열을 전부 추출 → B2C 목록 엔드포인트 확정.
3. Playwright는 **불필요**해서 시도하지 않음(1번으로 해결). 시스템 라이브러리 sudo 문제를 피했다.

확정 사항:

| 항목 | 값 |
|---|---|
| 엔드포인트 | `POST https://api.kcar.com/bc/search/list/drct` (K카 직영) |
| 총건수 조회 | 같은 엔드포인트에 `countFlag:true` |
| 페이징 | `pageno`(1-base), `limit`(100까지 확인) |
| 응답 | `data.data.rows[]`, `data.data.totalCnt`, `data.data.totalPageCnt` |
| 인증 | 불필요(비로그인 공개 목록) |

파라미터 인코딩: 요청 본문은 `{"enc": base64(AES-128-CBC(JSON))}`. 키·IV가 프런트 번들에 그대로 하드코딩돼 있다(`setEncDef` → `setEnc(data, "SKFJ2424DasfaJRI", "sfq241sf3dscs321")`, CryptoJS AES/CBC/Pkcs7). 인증·접근제어가 아니라 단순 파라미터 직렬화 포맷이며, 브라우저가 보내는 것과 동일한 요청을 재현한 것이다.

서버측에서 실제로 동작하는 필터(검증 완료, `countFlag`로 건수 비교):

| 파라미터 | 형식 | 확인 |
|---|---|---|
| `wr_lt_prc` | 만원, 상한 | 전체 7,361 → `1300` 적용시 1,969 |
| `wr_gt_mfg_dt` | `YYYYMM`, 하한 | + `201701` → 1,093 |
| `wr_lt_milg` | km, 상한 | + `120000` → **966** |
| `wr_in_mnuftr_nm` | `현대\|기아` | **무시됨**(건수 불변) → 제조사·연료·차종·사고는 응답 필드로 로컬 필터 |

탭 선택: `\/bc\/search\/list\/drct`(직영 7,361대)만 사용. `\/acm`(제휴딜러 749대)·`\/rent`(렌터카)·C2C(`\/api\/v1\/ds\/*`, 개인거래)는 제외 — 지시서의 "K카 진단 기준 사고 이력"과 "렌터카·영업용 제외"에 맞는 건 직영 차량이다.

목록 응답이 이미 84개 필드를 담고 있어 상세 페이지 없이도 지시서 2-2 필드 대부분을 채운다: `carCd, carWhlNm, mnuftrNm, modelGrpNm, modelNm, grdNm, grdDtlNm, mfgDt, prdcnYr, milg, prc, fuelNm, trnsmsnNm, engdispmnt, carctgrNm, acdtHistCnts, optnNm, cntrNm, cntrRgnNm, useNm, rentRegYn, msizeImgPath` 등.

상세 페이지가 Disallow라 채울 수 없는 필드: `total_cost`(K카 표시 총 구매비용), `insurance_history`, `owner_changes`, `listed_date`, `warranty`. → CSV에 `null`. 총 구매비용은 대신 `total_cost_est`(차량가 + 취득세 7% + 매도·이전대행 추정 3만원 단위 보정)를 **추정치로 명시**해 예산 상한 검증에만 쓴다.

## 2. 마일스톤 2 — 수집 완료

`python3.11 search_kcar.py --budget 1300 --year 2017 --km 120000 --fuel gasoline,hybrid,lpg,diesel`

- 요청 10회(페이지당 100건), 간격 1.8초, 순차. 차단·캡차·403/429 **없음**(전부 HTTP 200).
- 수집 968대(API `totalCnt` 968과 일치, 중복 0) → 조건 통과 **319대**.

제외 내역:

| 사유 | 건수 |
|---|---|
| 제조사(현대·기아·제네시스 아님) | 358 |
| 차종 제외(경차 267·화물 8·승합 2 등) | 232 |
| 사고 이력('사고') | 50 |
| 렌터카 트림 표기 | 7 |
| 연료(순수 전기) | 2 |
| **통과** | **319** |

### 막힌 점 2-1: `useNm`("영업용")을 이력으로 잘못 읽어 118대를 잘못 걸러냈다

1차 구현에서 `useNm`에 '영업용'이 있으면 제외했더니 통과가 208대로 줄고 **연료가 전부 가솔린**이 됐다. 968대 교차표를 뜯어보니 원인이 나왔다:

| 연료 | '영업용' 태그 | 비율 |
|---|---|---|
| LPG | 36/36 | 100% |
| 하이브리드(가솔린+전기) | 26/26 | 100% |
| 디젤 | 183/183 | 100% |
| 가솔린 | 0/453+ | 0% |

`useNm`/`useCd`는 연료로 100% 결정되는 **K카의 추천 용도 태그**(연료 특성상 사업용 주행에 적합)이지 차량 이력이 아니다. 같은 차들의 실제 플래그는 전부 `rentRegYn=N`, `regType=SELL`, `rentType=''`이다. → `useNm` 기반 제외를 **제거**하고 `use_tag`로 이름을 바꿔 CSV에만 참고 기록.

### 막힌 점 2-2: 실제 렌터카는 차명·트림에 박혀 있었다

`rentRegYn`은 968대 전부 'N'이라 쓸모가 없었다. 대신 `carWhlNm`/`grdNm`에 표기가 있었다: `더 뉴 K5 2세대 2.0 LPI **렌터카** 프레스티지`, `아반떼 AD 1.6 LPI 스마트**(렌터카)**`, `그랜저 IG 3.0 LPi 모던 **(렌터카용)**` — 8대(그중 1대는 사고로 이미 제외) → 차명 문자열로 제외.

시도했다가 버린 대안: `simcDesc`(판매자 코멘트)의 '렌트' 매칭. 16건 전부 `렌트이력無`·`렌트이력없음`·`렌트이력X` 같은 **긍정** 문구여서 그대로 쓰면 정반대로 걸러진다. `'리스'` 매칭도 `코란도 투리스모`가 오탐이라 폐기.

## 3. 마일스톤 3 — 시세 비교·점수

- 그룹: `model`(세대 단위, 예 '더 뉴 아반떼 AD') + 연식 ±1년. 매물별 슬라이딩 윈도로 계산(고정 버킷이 아니라 지시서의 "±1년"을 그대로 구현).
- 그룹 3대 미만이면 비교 불가 표시 + 절대값만: **27대**.
- 그룹 내 가격 산포가 중앙값의 60%를 넘으면 트림 계열(`grdNm` 첫 토큰)로 세분하고 `group_note`에 표시.
- 점수: 가중치를 `search_kcar.py` 상단 상수로 분리. `price_gap`은 중앙값 대비 **20% 저렴 = 만점**, `km_gap`은 **30% 덜 탐 = 만점**으로 선형 정규화(절대 금액이 아니라 비율이어야 700만원 차와 1,300만원 차를 같은 척도로 비교할 수 있다).

점수에서 판정 불가 항목 2개(지시서 명시 항목이지만 상세 페이지가 robots Disallow):

- 소유자 변경 1회 이하 10점 → 전 매물 0점
- 등록 7일 이내 +3점 → 전 매물 0점(목록 응답에 등록일 필드 없음)

**전 매물에 동일하게 적용되는 상수 오프셋이므로 순위에는 영향이 없다.** 억지로 100점 환산하지 않고 지시서 척도를 그대로 두었다(실질 만점 85점). 두 항목은 각 TOP 매물의 "가서 확인할 것"에 넣는다.

## 4. 마일스톤 4 — 사이트 빌드

`build_report.py` → `docs/report.md` + `docs/index.html`. 문구("왜 좋은지"·"가서 확인할 것")까지 전부 규칙 기반이라 LLM 없이 재생성된다.

사이트 규칙 이행: 단일 HTML(인라인 CSS/JS, 외부 스크립트·폰트 0개), 표 머리글 클릭 정렬, 차명·차종·연료 필터, 모바일 반응형, `<meta name="robots" content="noindex,nofollow">`, 사진 미게시, 상단에 수집 일시·출처·면책·수집 범위 고정.

버그 2건 수정:

- `location` 이 `영등포직영점(서울` 로 깨졌다. `f"{지점}({지역})".strip("()")` 가 양쪽 끝 문자를 지우는 `strip` 특성상 닫는 괄호까지 먹었다 → `" ".join(...)` 으로 교체.
- 트림 혼재 판정 임계값이 60%여서 정작 가장 심한 그룹을 놓쳤다. 실측 산포 분포(p50 23% / p75 31% / p90 35%)를 뽑아 **40%** 로 재조정 → 스토닉 2017±1(56%)·2018±1(50%)만 걸리고, 이 두 그룹이 실제로 1.0터보·1.4가솔린·1.6디젤이 섞인 경우였다. 세분 후 스토닉 1.6디젤 매물의 순위가 3위→6위로 내려갔다(같은 배기량끼리 비교하니 가격 이점이 줄어든 것이 맞다).

## 5. 마일스톤 5 — 검증 (`verify.py`, 전부 통과)

**상세 페이지 대조 문제**: 지시서 2-6은 "TOP 15 중 3개를 실제 상세 페이지와 대조"를 요구하지만 상세 페이지는 robots.txt Disallow다. 준수 규칙이 우선이므로 상세 페이지를 열지 않고, 대신 허용된 목록 API에 `wr_in_car_cd`로 **차량 단건 조회를 새로 보내** 대조했다. 페이지네이션 캐시를 다시 읽는 것이 아니라 독립 요청이므로 파싱·수집 오류를 실제로 잡아낸다. 사람이 눈으로 볼 상세 URL은 함께 출력한다.

무작위 3대(seed 20260908) 재조회 결과 — 차량가·주행거리·사고이력·연식·차명 **5개 필드 전부 일치**:

| 매물 | 차량가 | 주행거리 | 사고 | 연식 |
|---|---|---|---|---|
| EC61355237 기아 스토닉 1.4 가솔린 트렌디 | 950 = 950 | 108,377 = 108,377 | 무사고 = 무사고 | 2018-03 = 2018-03 |
| EC61379873 기아 스토닉 1.4 가솔린 트렌디 | 990 = 990 | 118,530 = 118,530 | 무사고 = 무사고 | 2018-06 = 2018-06 |
| EC61399288 현대 코나 디젤 1.6 2WD 모던 | 1,070 = 1,070 | 71,143 = 71,143 | 무사고 = 무사고 | 2017-10 = 2017-10 |

HTML 파서(stdlib `html.parser`) 검사:

- 전체 표 `#all` 행 수 **319 = CSV 319행** 일치. TOP15 표 15행, 세단·SUV·하이브리드 각 5행, 시세 그룹표 41행.
- `noindex,nofollow` 메타 존재 / `<img>` **0개** / 외부 스크립트 **0개** / 외부 폰트·CSS **0개** / 사진 URL(`img.kcar.com`) 미게시 / 필수 문구('개인 검토용', '상업적 이용 금지', 'kcar.com', '수집 일시') 전부 포함.

시세 그룹 이상치(사이트·리포트에 표시함):

| 상태 | 건수 |
|---|---|
| 트림계열로 세분(가격 산포 큼) | 25대 |
| 비교 불가(그룹 2대) | 17대 |
| 비교 불가(그룹 1대) | 10대 |

## 6. 시도했지만 쓰지 않은 대안 정리

| 막힌 지점 | 시도 | 결과 |
|---|---|---|
| SPA라 목록 HTML에 매물 없음 | (1) `/bc/search` HTML의 `__NUXT__` 파싱 | 목록 데이터 없음(클라이언트 페치) |
| | (2) `/_nuxt/*.js` 번들에서 엔드포인트 grep | **성공** — Playwright 불필요, sudo 의존성 회피 |
| | (3) Playwright headless | 시도 안 함(2로 해결) |
| 제조사 서버 필터 | `wr_in_mnuftr_nm=현대\|기아` | 서버가 무시(건수 불변) → 로컬 필터로 전환 |
| 영업용 이력 판정 | `useNm` 의 '영업용' | 연료로 100% 결정되는 추천 태그였다 → 폐기 |
| 렌터카 판정 | `rentRegYn` | 968대 전부 'N' → 무용 |
| | `simcDesc` 의 '렌트' | 16건 전부 '렌트이력無' 긍정 문구 → 폐기 |
| | 차명·트림의 '렌터카' 표기 | **성공** — 8대 검출 |
| 상세 필드(보험이력·소유자변경 등) | 상세 페이지 조회 | robots Disallow → **하지 않음**. null 처리 + 링크만 게시 |

## 7. 어머님용 안내 페이지 (추가 요청)

점수 위주 표 대신 **읽고 고를 수 있는 페이지**를 만들었다. `docs/index.html` 이 어머님용 안내가 되고, 기존 분석 표는 `docs/all.html` 로 옮겼다.

### 7-1. 차종 지식을 붙였다 (`car_knowledge.py`)

트림 문자열에서 엔진과 변속기를 판별해 매물마다 확인 항목을 만든다. 42개 차종 319대 분류 결과:

| 구분 | 대수 |
|---|---|
| 일반 자동변속기 | 205 |
| **건식 DCT** | 81 |
| 하이브리드 DCT | 17 |
| 무단변속기(IVT) | 16 |
| **세타2 GDI 계열 엔진** | **33** (그랜저 IG 2.4 × 21, 올 뉴 K7 2.4 GDI × 9, 그랜저 HG240 × 2, 쏘나타 2.0 터보 × 1) |

변속기는 **기어 단수를 단정하지 않고** 위 4가지로만 분류했다. 판단에 실제로 영향을 주는 구분은 단수가 아니라 건식 DCT인지 여부이고, 단수까지는 확신할 수 없기 때문이다.

### 7-2. 정보의 출처를 등급으로 구분했다

| 등급 | 내용 | 예 |
|---|---|---|
| FACT | 출처로 확인한 사실 | 세타2 2.4 GDI(G4KJ) 적용 차종·연식, 크랭크샤프트 금속 이물로 인한 베어링 조기 마모, 2015·2017·2020년 리콜, 미국 집단소송 합의의 평생보증·KSDS (위키피디아 Hyundai Theta engine / hbsslaw.com) |
| COMMON | 널리 알려진 정비 상식 | 건식 DCT 저속 울컥거림, GDI 오일 감소, 디젤 DPF, 10만km 정비 항목 |
| CALC | 이 스크립트의 계산 | 자동차세, 연간 유류비 |

**COMMON 항목은 "이 차에 그 고장이 있다"고 단정하지 않고 "확인할 것"으로만 제시한다.** R엔진(2.0/2.2 디젤) EGR·화재 관련 통설은 위키피디아 R engine 문서에서 확인되지 않아 **본문에서 주장하지 않고** 차대번호 리콜 조회로 넘겼다.

### 7-3. 검색 도구 한계

일반 웹검색으로 차종별 고질병을 조사하려 했으나 품질이 낮았다. `세타2 GDI` 검색에 GDP·GDI 경제 지표와 Kärcher K5 고압세척기가 섞여 나오고, `그랜저 IG 고질병` 검색에 IG증권·인스타그램이 반환됐다. 그래서 커뮤니티 크롤링 대신 **위키피디아 엔진 문서와 집단소송 공지처럼 검증 가능한 1차 자료로만 FACT를 채우고**, 나머지는 COMMON으로 표시하는 방식을 택했다.

### 7-4. 발견해서 고친 것

- **카니발 9인승·11인승 자동차세를 승용 기준으로 잘못 계산**했다(521,400원). 이 차들은 승합자동차로 등록돼 배기량과 무관한 정액이다 → 50,700원으로 수정. 40만원 이상 차이가 나고, 카니발을 고를 이유가 되는 정보였다.
- 추천에 같은 차종이 반복됐다(상위 3대가 전부 스토닉) → 차종당 1대로 제한.
- '타고 내리기 편한 차'에 디젤이 추천됐다. 같은 페이지에서 단거리에 디젤을 권하지 않으므로 모순 → 해당 항목에서 디젤 제외.
- 추천 목록에 경고가 있는 매물이 섞였다 → 추천에서는 경고 있는 매물 전면 제외.
- 프라이드·엑센트를 준중형으로 분류 → 소형 승용으로 수정.

### 7-5. 검증 (`verify.py` 확장, 전부 통과)

가이드 페이지에 대해: noindex 메타 / `<img>` 0개 / 외부 스크립트 0개 / 외부 폰트·CSS 0개 / 사진 URL 미게시 / 출처·수집일시·면책 문구 / 미수집 필드 한계 명시 / 리콜 조회 안내 / 태그 종결 확인. 분석 표 `docs/all.html` 은 여전히 319행 = CSV 319행 일치.

## 8. 전면 재작업 (2차 요청: 5인승 기준 · 전체 재고 · 파싱 재검토)

### 8-1. 5인승만 남겼다 — 특수 규칙 없이 `pasngrCnt` 로

카니발을 빼기 위해 차명 예외를 두는 대신 목록 응답의 `pasngrCnt`(승차정원)로 걸렀다. 968대 표본에서 이 필드는 신뢰할 수 있었다(5인승 886, 2인승 41=캐스퍼 밴, 7인승 13, 9인승 11, 11인승 2, 12인승 1=승합, 3인승 3=화물, 1·4인승=경차).

`--seats 5` 하나로 카니발(7·9·11인승)·카렌스(7인승)·싼타페 2.2 7인승·더 뉴 벨로스터(4인승)·화물·승합이 전부 정확히 빠진다. 차종 `미니밴` 제외도 함께 명시했다.

### 8-2. 직영 재고 **전체**를 받았다 (968대 → 7,408대)

이전에는 서버 필터(가격·연식·주행거리)를 걸어 968대만 받았다. 그러면 중앙값이 "1,300만원 이하 차들끼리의 중앙값"이 되어 시세가 실제보다 낮게 잡히고, 비교군도 얇아진다.

전체 재고 7,408대를 받아(75페이지, 1.8초 간격, 138초, 차단 0건) 그중 6,842대를 시세 비교군으로 쓰고 조건 필터를 그 다음에 걸었다.

| | 이전 | 이후 |
|---|---|---|
| 수집 | 968대 (서버 필터 후) | **7,408대 (전량)** |
| 시세 비교군 | 968대 | **6,842대** |
| 조건 통과 | 319대 | 304대 (5인승·미니밴 제외 반영) |
| **시세 비교 불가** | **27대** | **2대** |

### 8-3. 비교군 산정에서 실제 오류 2건을 찾아 고쳤다

**오류 1 — 트림이 섞여 시세차가 부풀려졌다.** 쏘나타 DN8 비교군을 열어보니 최하 트림 '스마트'(1,170만)와 최상 트림 '인스퍼레이션'(2,220만)이 한 그룹이었다. 중앙값 1,735만원이 나오면서 하위 트림 매물이 실제보다 훨씬 싸 보였다.

**오류 2 — 트림 식별자를 잘못 골랐다.** `trim_series = grdNm.split(" ")[0]` 를 썼는데, 이건 첫 토큰만 본다. 스토닉은 `grdNm`이 "1.4 가솔린"·"1.6 디젤"이라 첫 토큰이 엔진을 잘 갈랐지만, 쏘나타 DN8은 전부 "2.0 가솔린"이라 트림이 전혀 갈리지 않았다. **트림 등급은 `grdNm`이 아니라 `grdDtlNm`에 들어 있었다**(예: `grdNm`="2.0 가솔린", `grdDtlNm`="스마트"/"인스퍼레이션").

고친 방식 — 비교군을 좁은 쪽부터 단계적으로 후퇴시킨다:

1. 같은 세대·좌석수·연식±1년 + 연료 + 배기량(200cc 버킷) + `grdNm` + `grdDtlNm`
2. …+ `grdNm` 까지 (트림 등급 섞임)
3. …+ 배기량까지
4. …+ 연료까지
5. 세대만

3대 이상 남는 **가장 좁은 그룹**을 쓰고, 어느 단계를 썼는지 `group_basis` 에 기록한다.

| 비교 기준 | 대수 |
|---|---|
| 같은 세대·연료·배기량·트림 | 236 |
| 같은 세대·연료·배기량(트림 섞임) | 33 |
| 같은 세대·연료·엔진(트림 등급 섞임) | 24 |
| 같은 세대만 | 7 |
| 같은 세대·연료 | 2 |
| 비교불가 | 2 |

검산 — 쏘나타 DN8 '프리미엄 패밀리' 1,270만원의 비교군이 **동일 트림 7대**로 좁혀졌고, 그 안에서 최저가였다(주행거리 76,886km인 동일 트림 차가 1,690만원). 시세차 +25%는 실제였다.

**트림까지 맞추지 못한 매물은 안내 페이지에서 시세차를 주장하지 않는다.** 하위 트림이 과대평가되기 때문이다. 대신 "같은 트림 차가 적어 시세차를 신뢰할 수 없다"고 표시하고 정렬에서도 가격 이점을 빼며, 상위 추천에서 제외한다.

### 8-4. 웹검색 대신 데이터에서 관점을 세웠다

1차 작업에서 웹검색이 부실했다(`세타2 GDI` → GDP 경제지표·고압세척기, `그랜저 IG 고질병` → IG증권·인스타그램). 커뮤니티를 더 긁는 대신 **판단 근거를 네 가지로 한정**했다.

| 근거 | 내용 |
|---|---|
| 확인된 사실 | 세타2 GDI 리콜(위키피디아 Hyundai Theta engine, 미국 집단소송 합의 공지) |
| 구조적 사실 | 터보는 터보차저·인터쿨러가 추가되고, 건식 DCT는 클러치가 소모품이다 — 의견이 아니라 구조 |
| **전체 재고 통계** | 재고 7,408대에서 차종별 물량·순위(192종)와 세대별 연식 시세(429종)를 직접 계산 |
| 계산값 | 자동차세, 연간 유류비 |

특히 **차종별 재고량**을 새 판단 축으로 넣었다. 재고가 많은 차종은 부품 수급·정비소 경험·재판매가 모두 유리하다. 커뮤니티 글보다 실제 유통 물량이 확실한 근거다. 예: 그랜저 574대(1위), 쏘나타 277대(6위), 아반떼 180대(9위) vs 프라이드 7대(91위), 베뉴 8대(85위), i30 12대(75위). 재고 15대 미만은 "드문 차"로 표시하고 부품·재판매 불리를 경고한다.

세대별 연식 시세는 각 매물 카드에 막대로 붙여 감가 흐름을 눈으로 보게 했다(주행거리·트림이 섞여 완전한 감가 곡선은 아니라고 명시).

### 8-5. 추천 정렬에서 가격 비중이 낮아 잘못 추천되던 문제

상위 추천 3대 중 2대가 시세보다 3~9% **비싼** 차였다. 정렬식에서 `price_gap` 가중치가 주의 항목 1개의 절반밖에 안 됐던 탓이다. 가중치를 12로 올려 10% 저렴이 주의 1개보다 큰 이점이 되게 하고, 상위 추천에는 시세보다 비싼 차와 트림 비교가 부정확한 차를 넣지 않도록 했다. 시세보다 3~10% 비싼 경우도 주의로 잡는다(이전엔 10% 초과만 경고).

### 8-6. 재검증 (전부 통과)

- 무작위 3대 단건 재조회: 차량가·주행거리·사고·연식·차명 **15개 항목 전부 일치**
- 좌석 수: 304대 전부 5인승 / 미니밴·카니발 0대
- 상위 추천: 시세보다 비싼 차 0대, 트림 비교 부정확 0대
- `docs/all.html` 표 304행 = CSV 304행
- 가이드·표 두 페이지 모두: noindex / `<img>` 0개 / 외부 스크립트·폰트 0개 / 사진 URL 미게시 / 출처·수집일시·면책·미수집 필드 한계 명시

## 9. 3차 요청 — 다른 구매처 조사 · 2020년 이후 · 안전 우선

### 9-1. 구매처 조사 결과 (robots.txt 먼저 확인)

| 구매처 | robots.txt | 결과 |
|---|---|---|
| K카 직영 | 목록 Allow / 상세 Disallow | **수집** (전체 재고 7,408대) |
| **엔카** | `api.encar.com` → **`Disallow: /`** / `car.encar.com` → `Disallow: /api`, `/v1/readside/`, 상세 `/dc/dc_cardetailview.do`. 추가로 `User-agent: GPTBot → Disallow: /` | **수집하지 않음** — 매물 데이터 경로가 명시적으로 금지돼 있다 |
| 현대·제네시스 인증중고차 | `Allow: /` (Disallow 없음) | 규칙상 허용이지만 목록 엔드포인트가 자동 요청을 거부 → **못 받음** |
| KB차차차 | 대부분 허용 | 목록이 JS 로딩이라 추가 역설계 필요 → 이번엔 안 함 |

엔카는 지시서에서도 금지였고 robots.txt도 금지이므로 두 근거가 일치한다. 사용자가 요청했으나 **robots.txt 준수가 우선**이라 수집하지 않고, 조건이 적용된 검색 링크를 페이지에 실었다.

현대 인증중고차 조사 경과: `/p/search/vehicle` 페이지에는 매물이 없고(필터 UI만), 프런트 JS(`search-vehicle.js`)에서 `POST /p/search/vehicle/list`(HTML 조각), `/p/search/vehicle/element`, `/api/search/v2/filter/count`(JSON)를 찾았다. 파라미터 24개를 브라우저와 동일하게 맞춰 보냈으나 목록 엔드포인트는 계속 HTTP 400("페이지를 찾을 수 없습니다")을 반환했다. count API는 JSON으로 응답하며 `유효하지 않은 판매상태 파라미터입니다`를 돌려줬는데, 판매상태 코드 후보(20/10/30/공백)를 다 시도해도 통과하지 못했다. **10회 남짓 시도한 뒤 중단**했다(접근 통제를 두드리는 행위가 되지 않도록). 안내 페이지에는 "안전·보증만 보면 제조사 인증중고차가 가장 낫다"는 설명과 직접 링크를 넣었다.

### 9-2. 조건 변경 (2017년 이후·1,300만 → 2020년 이후·2,200만)

캐시된 전체 재고 7,408대를 그대로 재필터해 **추가 요청 없이** 반영했다.

| | 이전 | 이후 |
|---|---|---|
| 연식 | 2017년 이상 | **2020년 이상** |
| 차량가 | 1,300만원 이하 | **2,200만원 이하** |
| 주행거리 | 12만km 이하 | **10만km 이하** |
| 조건 통과 | 304대 | **490대** |
| 연식 분포 | 2017~2020 | 2020:172 / 2021:155 / 2022:119 / 2023:35 / 2024:9 |
| 가격 중앙값 | 1,090만원 | **1,890만원** |
| 주행 중앙값 | 약 9만km | **70,825km** |

### 9-3. 안전을 1급 판단축으로 올렸다 (옵션 데이터에서 직접)

목록 응답의 `optnNm`을 파싱해 안전장치를 뽑았다. 웹검색 없이 데이터로 판단할 수 있는 항목이다.

| 항목 | 490대 중 |
|---|---|
| 첨단 안전장치 4가지 전부(자동긴급제동·차선이탈경보·후측방경보·스마트크루즈) | **206대** |
| 3가지 | 106대 |
| 2가지 | 108대 |
| 1가지 이하 | 70대 |
| 에어백 4종(운전석·조수석·사이드·커튼) | **485대** |

- `ADAS_OPTIONS`·`PARKING_AIDS`·`AIRBAG_KINDS` 상수를 추가하고 `adas_n`·`airbags`·`parking_aid_n` 필드를 CSV에 실었다.
- 정렬에서 안전장치 1가지당 가중치를 크게 뒀고, **추천 목록에는 첨단 안전장치 2가지 미만인 차를 넣지 않는다**(`MIN_ADAS_FOR_PICK`).
- 새 카테고리 "안전장치가 가장 많은 차"를 맨 앞에 뒀다.
- 각 장치가 무엇을 해 주는지 한 줄로 풀어 썼다(예: 자동긴급제동 = 앞차를 못 봤을 때 차가 스스로 브레이크를 잡아 줌).

### 9-4. 엔진 판별 오류 수정 (신형 차에서 드러남)

2020년 이후 차로 바꾸자 기존 판별식의 오류가 드러났다.

- **2.5 GDI가 "3.0~3.3 가솔린"으로 잡혀 92대나 됐다.** 배기량 fallback이 `cc >= 2300 → 람다 3.0/3.3` 이었기 때문이다. 더 뉴 그랜저 2.5·싼타페 2.5·K8 2.5가 모두 오분류됐다.
- 트림 문자열 위주 판별을 **배기량(`engdispmnt`) 위주**로 다시 썼다. 배기량은 응답의 실측값이라 트림 문자열보다 신뢰할 수 있다. 자연흡기/터보/디젤별로 배기량 구간표를 두고, 터보 여부만 트림에서 읽는다.
- 엔진 추가: 2.5 가솔린, 3.5 가솔린, 2.0 터보, 3.3 터보.
- **변속기를 차급으로 나눴다.** 현대·기아는 소형·준중형에 건식 7단 DCT를 얹고 중형 이상에는 토크컨버터 자동을 쓴다. 그래서 쏘나타 DN8 1.6터보는 일반 자동, 셀토스 1.6터보는 건식 DCT로 갈린다. 확신하는 범위만 DCT로 표시한다.
- 세타2 GDI 판정에 **연식 상한(2020년)과 배기량 범위(2,340~2,380cc / 1,980~2,010cc)**를 넣었다. 2020년 이후 후보 490대에서 **세타2 판정 0대** — 스마트스트림으로 교체된 것이 맞게 반영됐다.
- 차종 지식에 셀토스·K8·G70·스팅어를 추가했다(이전엔 58대가 차급·성격 미매칭).

검증 10건 전부 통과: 더 뉴 그랜저 2.5→2.5 가솔린/일반자동, 그랜저 IG 2.4(2019)→세타2/일반자동, K8 3.5→3.5 가솔린, 셀토스 1.6T→1.6터보/건식DCT, 아반떼 CN7 1.6→1.6/IVT, 쏘나타 DN8 1.6T→1.6터보/일반자동, 투싼 4세대 1.6디젤→건식DCT, 싼타페 2.2디젤→일반자동, 니로→하이브리드DCT.

### 9-5. 신뢰할 수 없는 시세차를 카드에서도 명시

트림이 섞인 비교군의 시세차가 카드에 숫자로 그대로 보이던 문제를 고쳤다. 이제 같은 트림 비교일 때만 "같은 트림 N대 대비 +X% (저렴)"으로 쓰고, 그렇지 않으면 "트림이 섞여 있어 이 숫자는 믿을 수 없습니다"로 표시한다.

### 9-6. 재검증 (전부 통과)

추천 24대 전수 검사: 안전장치 2종 미만 0대 / 경고 있는 매물 0대 / 5인승 아님 0대 / 2020년 미만 0대 / 2,200만원 초과 0대. 무작위 3대 단건 재조회 15개 항목 일치. `docs/all.html` 490행 = CSV 490행. 두 페이지 사이트 규칙 전부 통과.

## 10. 감가 vs 중고방어 — 데이터로 답하기

사용자 질문: "정가가 비싼데 감가가 많이 된 차가 더 좋은 것 같기도 하다. 많이 떨어졌으면 더 떨어질 리 없으니까. 반대로 중고방어를 잘하는 차면 신차와 값 차이가 없어서 아까운 것 같기도 하다."

재고 6,842대에서 같은 세대의 인접 연식 중앙값 차이로 1년치 감가를 306개 관측(세대 130종)해 계산했다.

### 10-1. 맞는 부분 — 차령이 오래되면 감가액이 줄어든다

| 차령 | 1년치 감가액(중앙값) | 감가율 |
|---:|---:|---:|
| 3년 | 218만원 | 8.1% |
| 4년 | 120만원 | 6.0% |
| 6년 | 78만원 | 5.8% |
| 8년 | 42만원 | 4.0% |
| 10년 | 40만원 | 3.1% |

첫 3년이 가장 비싸고 그 뒤로 완만해진다. **가장 비싼 구간을 남이 부담한 차를 사는 것은 유리하다.** 다만 감가율(%)은 4~9년차에 6% 안팎으로 크게 줄지 않는다 — 금액이 줄어드는 건 차값 자체가 낮아졌기 때문이다.

### 10-2. 틀린 부분 — '원래 비싼 차'라고 감가가 멈추지 않는다

같은 재고를 **현재 시세대별로** 나누면 결론이 뒤집힌다(차령 3~6년 구간).

| 지금 시세 | 1년치 감가액 | 감가율 |
|---|---:|---:|
| 1,500만원 미만 | 90만원 | 7.2% |
| **1,500~2,500만원** | **80만원** | **4.4%** |
| 2,500~4,000만원 | 250만원 | 9.0% |
| 4,000만원 이상 | 450만원 | 10.3% |

비싼 차는 이미 많이 떨어진 뒤에도 **비율로도 더** 떨어진다. 사용자의 예산대(2,000만원 ±)가 마침 감가가 가장 작은 구간이다.

### 10-3. 결정적 비교 — 차값 순서와 소유비용 순서가 다르다

`연간 총 소유비용 = 3년 감가 ÷ 3 + 자동차세 + 기름값` 을 490대 전부 계산했다.

| 차종 | 차값(중앙값) | 연간 총 소유비용 |
|---|---:|---:|
| 제네시스 G80 3.3 | 1,920만원 | **468만원** |
| 기아 K8 | 2,115만원 | 359만원 |
| 현대 더 뉴 그랜저 | 2,055만원 | 348만원 |
| 제네시스 G70 | 1,990만원 | 315만원 |
| 현대 쏘나타 DN8 | 1,840만원 | 299만원 |
| 기아 더 뉴 K3 | 1,520만원 | 248만원 |
| 현대 아반떼 (CN7) | 1,550만원 | **245만원** |

**G80은 아반떼보다 차값이 370만원 비싼데 연 소유비용은 223만원 더 든다(3년 670만원).** 원인은 두 가지다: (1) 3년 감가가 540만원(28%) vs 258만원(17%), (2) 세금·기름값이 연 280만원 vs 170만원. "비싼 차를 싸게 샀다"고 생각한 금액이 유지비와 추가 감가로 다시 빠져나간다.

중고방어가 좋은 차는 살 때 비싸지만 **팔 때도 잘 받으므로 내 감가 손해는 오히려 적다.** 신차값과 비슷해 아까운 느낌이 들 뿐 손해가 아니다.

### 10-4. 구현

- `car_knowledge.forward_depreciation()` — 1순위로 같은 세대의 3년 더 오래된 연식과의 **실제 시세 차이**, 데이터가 부족하면 관측된 차령별 감가율(`AGE_DEPRECIATION_RATE`) 누적. 근거를 문자열로 함께 반환한다.
- 모든 매물 카드에 **3년 뒤 예상 감가**와 **연간 총 소유비용**을 표시.
- 새 카테고리 "3년 타고 팔 때 손해가 가장 적은 차" 추가.
- 3년 감가가 차값의 13% 이하면 좋은 점, 22% 이상이면 주의로 표시.

### 10-5. 이 과정에서 잡은 버그 2건

1. **기름값이 0원으로 계산됐다.** 새로 추가한 엔진(2.5 GDI, 2.0T, 3.5, 3.3T)이 `FUEL_ECONOMY` 표에 없어 `annual_fuel_cost()`가 `None`을 반환했다. 그 결과 그랜저 2.5·K7 2.5·스팅어 2.0T가 "유지비 가장 낮은 차" 상위를 차지하는 **잘못된 추천**이 나왔다. 연비 값을 채우고, 표에 빠진 엔진을 알려 주는 `missing_fuel_economy()` 점검 함수를 추가했다.
2. **세대 출시 연도 경계에서 감가가 4%로 과소평가됐다.** 2019년 11월에 나온 쏘나타 DN8 하이브리드의 2022년식을 2019년식과 비교하니, 2019년식이 사실상 2020년 초 차량이라 3년치 감가가 거의 0으로 잡혔다. 관측된 연 4~9% 감가와 모순되는 값(3년에 8% 미만)은 신뢰하지 않고 차령별 평균으로 되돌리도록 가드를 넣었다. 적용 후 3년 감가율 범위 8~30%(중앙값 17%)로 정상화.

## 11. 4차 요청 — 차종 범위 재설정 · 장기 보유 관점 · 전체 비교표

### 11-1. SUV 크기 상한(셀토스급)과 세단 무제한

"SUV는 셀토스 정도까지, 그랜저·K8은 괜찮고, 싼타페는 너무 비싸다"를 전장 기준으로 구현했다.

| | 전장 | 처리 |
|---|---:|---|
| 베뉴 | 4,040mm | 유지 |
| 스토닉 | 4,140mm | 유지 |
| 코나 | 4,165~4,350mm | 유지 |
| **니로** | **4,355mm** | 유지 — 셀토스보다 작아 **소형 SUV로 재분류**(이전엔 준중형 SUV) |
| 셀토스 | 4,375mm | 유지(상한) |
| 투싼 | 4,630mm | 제외 |
| 스포티지 | 4,660mm | 제외 |
| 싼타페 | 4,785mm | 제외 |
| 쏘렌토 | 4,810mm | 제외 |

`SMALL_SUV_ALLOW` 화이트리스트로 구현했다(블랙리스트보다 안전). 세단은 크기 제한을 두지 않았다 — 준대형 세단은 낮고 길어 주차 부담이 큰 SUV보다 작다는 판단이며, 사용자가 그랜저·K8을 명시적으로 허용했다.

후보 **490대 → 365대** (SUV가 셀토스보다 큼 1,567대 제외). 구성: 중형 세단 110 / 준대형 이상 세단 96 / 준중형 승용 90 / 소형 SUV 69(셀토스 26·니로 20·코나 16·베뉴 5·스토닉 2).

### 11-2. "다시 팔지 않는다" — 판단 기준을 바꿨다

사용자: "나는 다시 팔지 않아. 그럴 거면 좋은 트림으로 좋은 차(원래 비쌌던 차)를 사는 게 좋을 것 같다."

맞는 관점이다. **감가는 팔 때만 발생하는 손해**이므로 끝까지 타면 통장에서 나가지 않는다. 기준을 다시 세웠다.

| 항목 | 3~5년 뒤 팔 계획 | 끝까지 탈 계획 |
|---|---|---|
| 감가 | 가장 큰 비용 | **안 봐도 됨** |
| 트림·장비 | 되팔 때 회수 안 됨 | **가장 중요** — 매일 쓰고 나중에 못 바꿈 |
| 세금·기름값 | 보통 | **매우 중요** — 10년이면 10배로 쌓임 |
| 부품 수급 | 보통 | **매우 중요** |
| 안전장치 | 중요 | **매우 중요** — 노출 기간이 길다 |

새 지표를 추가했다.

- `trim_rank` / `trim_total` — 같은 세대의 트림별 시세 중앙값 순서로 트림 등급을 매긴다(예 `1/7` = 7개 트림 중 최상위). 328/365대에서 산출됐다.
- `equip_n` / `equip_total` — 추적하는 24가지 장비 중 몇 개인지. 분포 5~23개.
- `running_cost` = 자동차세 + 기름값 (**감가 제외**). 10년 누적도 함께 표시.
- 새 카테고리 "오래 타실 거라면 — 같은 돈으로 가장 좋은 차"를 최상단에 뒀다(장비·트림·안전이 높고 유지비가 낮은 순).

### 11-3. 데이터가 준 결론 — 트림은 높이고 배기량은 낮춰라

후보 365대를 배기량으로 나눠 10년 유지비를 계산했다.

| 배기량 | 대수 | 연 유지비 | 10년 누적 |
|---|---:|---:|---:|
| 1,600cc 이하 | 181 | 162만원 | **1,622만원** |
| 1,600~2,000cc | 88 | 212만원 | 2,119만원 |
| 2,000~2,600cc | 79 | 237만원 | 2,374만원 |
| 2,600cc 초과 | 17 | 280만원 | **2,799만원** |

**1,600cc와 2,600cc 초과의 10년 차이가 1,177만원**으로, 차값 차이보다 큰 경우가 많다. 그래서 "원래 비쌌던 좋은 차"를 고를 때 **트림은 높이고 배기량은 낮추는 조합**이 최적이다.

실제로 그런 차가 있다: **K5 3세대 1.6 터보 시그니처** — 그 세대 최상위 트림(1/7등급), 장비 22/24, 안전장치 4종인데 배기량이 1,600cc라 연 유지비 193만원. 같은 값대 3.0~3.5 대배기량보다 10년에 900만원 가까이 덜 든다.

차급별로도 확인됐다: 준중형 승용 10년 1,607만 / 소형 SUV 1,819만 / 중형 세단 2,119만 / 준대형 이상 세단 2,374만.

### 11-4. 전체 365대 한눈에 비교표

"그냥 전부 비교하고 확인할 수 있게, 한 번에 가독성 좋게" 요청에 맞춰 기존 '나머지 40대' 목록을 **전체 비교표**로 교체했다.

- 차급별로 4개 블록(소형 SUV 69 / 준중형 승용 90 / 중형 세단 110 / 준대형 이상 세단 96)
- 열: 차·트림 / 연식 / 주행 / 차값 / **트림등급** / **장비(24중)** / **안전장치** / **연 유지비** / **10년 누적** / 등급 / 링크
- 각 블록 안은 장기 보유 기준(장비·트림·안전 높고 유지비 낮은 순)으로 정렬
- 표 머리글 클릭 정렬, 차명·트림 검색, 연료 필터, 안전장치 개수 필터(4가지 전부 / 3가지 이상 / 2가지 이상), 표시 대수 실시간 표기
- 헤더 고정(sticky), 짝수 행 음영, 모바일 가로 스크롤
- 각 열이 무슨 뜻인지 표 위에 풀어 설명(트림등급 `1/7`의 의미, 감가를 넣지 않은 이유)

### 11-5. 이 과정에서 잡은 버그

**f-string 안에 JavaScript를 넣어 빌드가 실패했다.** 정렬·필터 JS의 중괄호가 f-string 치환으로 해석돼 `SyntaxError`가 났고, 실패한 뒤에도 이전 빌드 결과 파일이 남아 있어 겉보기로는 정상처럼 보였다(`sticky` 문자열이 0건인 것으로 발견). JS를 f-string 밖의 별도 상수 `CMP_JS`로 분리해 해결했다.

### 11-6. 재검증 (전부 통과)

비교표 행 수 검사를 `verify.py`에 추가했다: **가이드 비교표 365행 = CSV 365행** (cmp1 69 + cmp2 90 + cmp3 110 + cmp4 96). `docs/all.html` 365행도 일치. 무작위 3대 단건 재조회 15개 항목 일치. 두 페이지 사이트 규칙(noindex·사진 0·외부 스크립트 0·면책·미수집 필드 명시) 전부 통과. 투싼·스포티지·싼타페·쏘렌토 0대 확인.
