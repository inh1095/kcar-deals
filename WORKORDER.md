# K카 국산 중고차 꿀매물 탐색 — 작업 지시서 (랩 Claude, Bedrock)

작성 2026-09-08. 환경: 워크숍 랩 EC2(Amazon Linux)의 Claude Code on Bedrock. WebSearch 없음. 랩 계정은 **2026-09-10 08:20 KST 만료** — 그 전에 결과가 GitHub에 push돼 있어야 한다.
아래 조건에 맞는 매물을 K카(kcar.com)에서 수집·분석해 "지금 사면 좋은 매물" 리스트를 만들고, **GitHub Pages 사이트로 게시**한다. 질문하지 말고 끝까지 진행하고, 막히면 대안 2~3개를 시도한 뒤 결과와 함께 `PROGRESS.md`에 기록한다.

## 1. 검색 조건 (이 블록만 고쳐서 쓰면 됨)

```
사이트      : K카(kcar.com) 내차사기만 사용. 엔카·KB차차차 등 다른 사이트 사용 금지.
제조사      : 현대, 기아 (제네시스 포함)
차종        : 세단, 해치백, SUV, 미니밴 모두 허용
제외        : 경차(모닝, 레이, 캐스퍼, 스파크 등), 화물, 승합(버스), 렌터카·영업용 이력 차량
예산        : 차량가 1,300만원 이하 (총 구매비용 1,400만원 이하)
연식        : 2017년식 이상
주행거리    : 12만km 이하
연료        : 가솔린, 하이브리드, LPG 허용. 디젤은 허용하되 감점.
사고        : 무사고 또는 단순 교환 1건 이하. 침수·전손·도난 이력 제외.
지역        : 전국 (홈서비스 배송 가능하므로 무관. 시승 참고용으로 지점만 기록, 점수에는 반영 안 함)
용도        : 출퇴근용, 편의 옵션 중시. 후방카메라·열선/통풍시트·스마트키·내비 있으면 가점.
```

## 2. 작업 순서

### 2-1. 사이트 구조 파악
- `https://www.kcar.com/robots.txt`와 이용약관을 먼저 확인하고 허용 범위 안에서만 작업. 확인 결과를 `PROGRESS.md`와 `report.md` 상단에 적는다.
- K카는 SPA다. 순서: (1) `curl`로 내차사기 페이지 HTML → `<script src>` 번들 JS를 받아 `fetch(`/`axios`/`/api/` 문자열을 grep해 매물 목록 API 엔드포인트와 파라미터(필터·페이지)를 찾는다. (2) 못 찾으면 Playwright(headless chromium)로 접속해 필터를 걸고 네트워크 요청에서 XHR/JSON을 찾는다. Playwright는 `pip install playwright && python -m playwright install chromium`로 시도하고, 시스템 라이브러리 부족(`--with-deps`에 sudo 필요)으로 실패하면 (1)로 돌아가 페이지 HTML·JS 분석으로만 해결한다.
- 엔드포인트를 찾으면 브라우저 없이 `requests`로 페이지네이션 재현. 요청 간격 1.5~2초, 동시 요청 금지, User-Agent는 일반 브라우저 값 하나로 고정. 차단(403/429/캡차)되면 우회하지 말고 멈추고 보고.
- 수집한 응답 원본은 `data/raw/*.json`에 저장(gitignore 대상 — 커밋하지 않는다).

### 2-2. 수집 필드
매물마다 아래를 뽑는다. 없으면 null.

| 필드 | 설명 |
|---|---|
| id, url | 매물 고유번호, 상세 페이지 링크(전체 URL) |
| model, trim | 차명, 트림 (예: K5 하이브리드 2세대 / 노블레스) |
| year_month | 연식 (예: 2017-11), 연형(18년형) 별도 |
| km | 주행거리 |
| price | 차량가 (만원) |
| total_cost | K카 표시 총 구매 예상 비용 (이전비·매도비 포함) |
| fuel, transmission, cc | 연료, 변속기, 배기량 |
| body_type | 세단 / SUV / 미니밴 / 해치백 |
| accident | K카 진단 기준 무사고 / 단순수리 / 사고 (내용 요약) |
| insurance_history | 보험 이력 (내 차 피해 n건, 타 차 가해 n건, 침수·전손 여부) |
| owner_changes | 소유자 변경 횟수 |
| options | 후방카메라, 통풍시트, 열선시트, 스마트키, 내비, 크루즈, 선루프 (있는 것만 리스트) |
| location | 지점 / 지역 (참고용) |
| listed_date | 매물 등록일 |
| warranty | K카 보증 / 연장보증 가능 여부 |
| photo | 대표 사진 URL 1개 (CSV에만 기록, 사이트에는 싣지 않는다) |

정리본은 `data/listings.csv`(커밋 대상). 상세 페이지 추가 요청이 필요한 필드(보험 이력·옵션 등)는 목록 API로 1차 필터를 통과한 매물에 대해서만 요청한다(요청 수 최소화).

### 2-3. 시세 비교 (K카 내부 데이터로만)
- 같은 `model + 연식(±1년)` 그룹으로 묶어 그룹별 가격 중앙값, 주행거리 중앙값 계산. 트림이 섞여 중앙값이 왜곡되면 `model + trim 계열`로 세분하고 그 사실을 표시.
- 각 매물: `price_gap` = (그룹 중앙값 − 매물 가격) / 그룹 중앙값 (클수록 저렴), `km_gap` = (그룹 주행거리 중앙값 − 매물 주행거리) / 그룹 중앙값 (클수록 덜 탔음).
- 그룹 매물이 3개 미만이면 비교 불가로 표시하고 절대값(가격, 주행거리)만 보여준다.

### 2-4. 점수 (꿀매물 판단)
0~100점. 가중치는 코드 상단 상수로 빼서 쉽게 바꿀 수 있게.
- 가격 이점 (price_gap): 30점
- 주행거리 이점 (km_gap): 20점
- 사고 이력: 무사고 20점 / 단순수리 10점 / 그 외 0점
- 소유자 변경 1회 이하: 10점
- 편의 옵션 (후방카메라·열선·스마트키·내비 각 2.5점): 10점
- 연료: 하이브리드 +5, 가솔린/LPG 0, 디젤 −5
- 등록 7일 이내 신규 매물: +3 (경쟁 붙기 전)

### 2-5. 결과물 (전부 이 리포에 커밋)
1. `search_kcar.py` — 수집 + 시세 비교 + 점수 계산. **LLM 없이** 재실행 가능. 예: `python search_kcar.py --budget 1300 --year 2017 --km 120000 --fuel gasoline,hybrid,lpg --out data/listings.csv`
2. `build_report.py` — `data/listings.csv` → `docs/report.md` + `docs/index.html`. 사이트 규칙: 정렬 가능한 표, 모바일 반응형, 매물 링크 클릭 가능, 사진 없음, `<meta name="robots" content="noindex,nofollow">`, 상단에 수집 일시·출처(K카)·면책("개인 검토용 비공식 분석, 가격·매물 상태는 수시 변동, 구매 판단은 매물 페이지에서 직접 확인, 상업적 이용 금지"). 외부 스크립트·폰트 없이 단일 HTML(인라인 CSS/JS).
3. `docs/report.md` — 전체 요약(수집 매물 수, 조건 통과 수, 그룹별 시세표) / **종합 TOP 15** 표(순위·차명·트림·연식·주행거리·차량가·총비용·사고·점수·링크) / **세단 TOP 5 · SUV TOP 5 · 하이브리드 TOP 5** / 각 TOP 매물마다 두 줄: "왜 좋은지" · "가서 확인할 것"(단순수리 부위, 소유자 변경 2회, 타이어 마모 등).
4. `README.md` — 실행 방법 3줄, 조건 바꾸는 방법, 사이트 주소.
5. `PROGRESS.md` — 마일스톤·막힌 점·해결·검증 결과.

GitHub Pages는 `main` 브랜치 `/docs`에서 서빙된다: https://inh1095.github.io/kcar-deals/ (push 후 1~2분).

### 2-6. 검증
- TOP 15 중 무작위 3개를 실제 상세 페이지와 대조해 가격·주행거리·사고 이력이 맞는지 확인하고 `report.md`와 `PROGRESS.md`에 적는다.
- 시세 그룹 계산이 이상한 것(트림 섞임 등)이 있으면 표시.
- `docs/index.html`을 브라우저 없이라도 HTML 파서로 열어 표 행 수가 CSV와 일치하는지 확인.

## 3. 주의
- 개인 구매 검토 목적의 비공식 분석. 원본(`data/raw/`)은 작업 환경 안에만 두고 커밋하지 않는다. 공개하는 것은 분석 표뿐이다.
- 이용약관·robots.txt 위반 소지가 있으면 그 지점에서 멈추고, 수동으로 볼 수 있는 검색 URL(필터 적용된 링크) 목록을 `docs/`에 대신 게시하고 보고한다.
- 매물은 시간이 지나면 팔리므로 report와 사이트 상단에 수집 일시를 표기한다.
- Playwright가 안 되면 requests + API 방식으로 전환. 인터넷이 아예 안 되면 즉시 멈추고 보고.

## 4. 최종 보고
1. 모든 결과물을 커밋해 `origin main`에 push하고 `git status`가 깨끗한지 확인.
2. 채팅에: 사이트 URL, 수집 매물 수 / 조건 통과 수 / 수집 일시, **TOP 15 표**(링크는 전체 URL), 막혔던 지점과 해결 방법. report 전문은 사이트에 있으니 채팅에는 TOP 15와 요약만.
